// Partners are now provisioned automatically per account on first use
// (see src/lib/partners.ts), so there is nothing to seed globally.
console.log("Nothing to seed: partners are created per account on first use.");
